name: Deploy Presentation to Pages

on:
  push:
    branches: ["main"]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: react-intern/package-lock.json

      - name: Install dependencies
        run: npm install
        working-directory: react-intern

      - name: Render Presentation
        run: |
          npm run render:html
          mv presentation.html index.html
        working-directory: react-intern

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: react-intern/

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
